apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Include all YAML manifests in this folder
resources:
  - podsecurity-restricted.yaml
  - podsecurity-subrule-baseline.yaml
  - require-requests-limits.yaml
  - require-signed-images.yaml